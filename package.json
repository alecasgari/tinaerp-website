{
  "name": "tina-erp-website",
  "version": "1.0.0",
  "private": true,
  "description": "سایت استاتیک تینا ERP — Eleventy + GitHub Pages",
  "scripts": {
    "start": "eleventy --serve --port=8080",
    "build": "eleventy",
    "clean": "rimraf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "rimraf": "^6.0.1"
  }
}
